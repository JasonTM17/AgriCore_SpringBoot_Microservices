# DO NOT put real passwords here. Create secret manually:
# kubectl create secret generic agricore-db --from-literal=username=agricore --from-literal=password=<secret>
apiVersion: v1
kind: Secret
metadata:
  name: agricore-db
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  username: "CHANGE_ME"
  password: "CHANGE_ME"
