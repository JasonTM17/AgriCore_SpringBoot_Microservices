openapi: 3.1.0
info:
  title: AgriCore Work Service API
  version: 1.0.0
  description: Field work tasks assignment and completion
servers:
  - url: http://localhost:8085
paths:
  /api/v1/work-tasks:
    post:
      summary: Create work task
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, cropCycleId, plotId, taskType, title]
              properties:
                code: { type: string }
                cropCycleId: { type: string, format: uuid }
                plotId: { type: string, format: uuid }
                taskType: { type: string }
                title: { type: string }
                priority: { type: string }
      responses:
        '201':
          description: Created
    get:
      summary: List work tasks
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: cropCycleId
          schema: { type: string, format: uuid }
        - in: query
          name: page
          schema: { type: integer, default: 0 }
        - in: query
          name: size
          description: Clamped to 100 by the service
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Paged work tasks
  /api/v1/work-tasks/{taskId}:
    get:
      summary: Get work task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: taskId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Work task detail
  /api/v1/work-tasks/{taskId}/assign:
    post:
      summary: Assign task to employee
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: taskId
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assignedEmployeeId]
              properties:
                assignedEmployeeId: { type: string, format: uuid }
      responses:
        '200':
          description: Assigned
  /api/v1/work-tasks/{taskId}/complete:
    post:
      summary: Complete task
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: taskId
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                notes: { type: string }
      responses:
        '200':
          description: Completed
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
